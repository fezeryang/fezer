
以下是完整计划。

/jianli Agent 修复 + 产品化拓展计划
总体结构：5 个阶段，修复是地基，产品功能按依赖顺序叠加
Phase 1 架构修复 - 地基
Phase 2 流式+思考可视化
Phase 3 协作可视化
Phase 4 内容卡片
Phase 5 访客进度
1.1 恢复内容感知路由
1.2 打通会话历史
1.3 消除重复用户消息
1.4 递归防护+白名单强制
1.5 grounding 转发
1.6 工具暴露策略重做
1.7 错误信封+杂项
2.1 llm.ts 流式通道
2.2 SSE 路由+事件协议
2.3 前端流式消费
并行路径激活+真实综合
协作时间线 UI
卡片 schema+确定性派生
卡片 UI+导航
localStorage 进度存储
进度注入+推荐去重
Phase 1 — 架构修复(全部 9 项审计发现)
1.1 恢复内容感知路由(CRITICAL-1)
目标：普通聊天走 LLM 意图分类；房间/点击只做偏置，不再绑架路由。

orchestrator/graph.ts callSupervisor 节点:interactionType === "click" || "guide" 时才传 preferredAgent,否则传 undefined
supervisor/graph.ts resolveContextualTargetAgent:删除 if (state.preferredAgent) 的无条件短路(110 行)，硬覆盖仅保留给 click/guide;chat 时把 roomId 作为软偏置传给分类器
intent-classifier.ts:classifyIntent(userInput, spatialHint?) — 提示词中加入"用户当前所在房间：X(仅作偏置参考，领域不匹配时忽略)";保留 quickClassify 快速路径
同步更新 graph.spatial.test.ts 和 intent-classifier.test.ts:新增"writer 房间问技术问题 → 路由 builder"用例
1.2 打通会话历史(CRITICAL-2)
shared/src/schemas/agent.ts:FrontendAgentRequest 增加 conversationHistory?: Array<{ role: "user" | "assistant"; content: string }>
ChatModal.tsx:handleSend 携带最近 8 轮；重置 effect(168-174 行)改为仅在 isOpen 由 false→true 时清空，切房间/换角色不再丢对话
链路透传:chat.ts → orchestrator state → supervisor → invokeAgent 的 options.context.conversationHistory
服务端信任边界：截断为最多 8 条、每条 4000 字符
agent-factory.ts:contextMessages.slice(-5) 改为消费结构化 conversationHistory
1.3 消除重复用户消息(HIGH-3)
expert 层消息数组改为完全从结构化字段构建(不再依赖 orchestrator 注入的 HumanMessage):

// agent-factory.ts invokeAgentInternal 内,替换 537-543 行
const messages: Message[] = [
  { role: "system", content: systemPrompt },
  ...(groundingPolicyMessages),
  ...(directToolContext ? [{ role: "system", content: directToolContext }] : []),
  ...conversationHistory.map(toLLMMessage),   // 不含当前这条
  { role: "user", content: input },           // 唯一一份当前输入
];
1.4 递归防护 + 白名单强制(HIGH-4)
agent-factory.ts:AgentInvokeOptions 增加 consultDepth?: number;consultDepth >= 1 时从动态工具中剔除 ask_other_agent/ask_multiple_agents(嵌套层不可再嵌套)
白名单强制在注入点:initializeAgentFactory 里包装 invoker,ask_other_agent 请求的 agentId 不在调用方 canConsult 列表时返回结构化拒绝(而非执行)
ask_multiple_agents 参与者上限 4 → 3,总数守卫(嵌套层不允许)
1.5 grounding 转发 + 死参数清理(HIGH-5)
agent.tool.ts:调用 invoker 时改传 options.context(含 grounding、conversationHistory),删除从未被读取的 previousContext 字段及其 schema 描述
1.6 工具暴露策略重做(MEDIUM-6/7)
删除 wantsDynamicTools 关键词闸门：按 AGENT_TOOL_CONFIGS[agentId].tools 白名单常驻暴露检索工具，模型可自主调用
get_profile_full 无条件预取(237 行)改为条件预取:wantsProfile || wantsGuide || agentId === "core" 时才取
TOOL_CONTEXT_CHAR_LIMIT 11000 → 6000,PROFILE_TOOL_RESULT_CHAR_LIMIT 9000 → 5000
1.7 错误信封 + 杂项(MEDIUM-9/LOW)
chat.ts 400 路径补 message: "请输入有效的问题" 字段
ChatModal.tsx 251 行：打字发送一律 interactionType: "chat","click" 仅在角色点击入口使用
消息 id 改 crypto.randomUUID()
synthesizeResponses 拼接改为显示名映射(Aries · Core 等，为 Phase 3 铺路)
visitedRooms/discoveredCharacters 死参数留待 Phase 5 接活(本阶段先在 schema 注释标注)
Phase 2 — 流式响应 + 思考过程可视化
2.1 LLM 流式通道
llm.ts 新增 invokeLLMStream(params): AsyncIterable<StreamChunk>:payload 加 stream: true,两 provider 均为 OpenAI 兼容 SSE;故障转移仅在首 chunk 前生效。非流式 invokeLLM 保持不动(意图分类等仍用它)。

2.2 SSE 路由 + 共享事件协议
shared/src/schemas/agent.ts 新增联合类型 AgentStreamEvent:

type AgentStreamEvent =
  | { type: "agent_start"; agentId: FezerType; displayName: string }
  | { type: "thinking_step"; label: string; toolName?: string; status: "running" | "done" | "error" }
  | { type: "text_delta"; content: string }
  | { type: "agent_done"; agentId: FezerType }
  | { type: "done"; response: AgentResponse }   // 完整信封,含 uiAction/cards
  | { type: "error"; message: string };
chat.ts:请求带 stream: true 时走 SSE(text/event-stream),否则保留现有 JSON 路径(向后兼容，E2E/旧前端不受影响)。事件源:agent-factory 工具循环注入 onEvent 回调 — 工具执行前后发 thinking_step,最终回答循环用 invokeLLMStream 发 text_delta。e2e-mock.ts 增加 SSE mock 变体。

2.3 前端流式消费
useAgentChat.ts:新增 sendMessageStream(fetch + ReadableStream 解析 SSE,AbortController 可取消);失败自动降级为旧 sendMessage
ChatModal.tsx + ThinkingIndicator.tsx:接入真实 thinking_step(工具名→中文标签映射表:get_profile_full → "正在查阅简历档案"),流式文本经 Streamdown 增量渲染，替换现在硬编码的两条假状态
Phase 3 — 多专家协作可视化
supervisor/graph.ts:Phase 1 后并行路径可达;executeParallelAgents 为每个参与者转发 agent_start/thinking_step/text_delta(带 agentId 区分)，参与者上限 3
synthesizeResponses 从拼接改为真实 LLM 综合调用(输入：原问题 + 各专家回答，要求融合而非罗列)，综合失败降级为带显示名的分段呈现
前端:ChatModal 内新增协作时间线(各 agent 头像 + 状态圆点：思考中/回答中/完成)，最终综合答案以 core 身份呈现、尾部标注参与者
门槛：仅意图分类 needsConsultation=true 触发，避免简单问题也并行烧 token
Phase 4 — 聊天内嵌内容卡片
shared/src/schemas/agent.ts:AgentResponse 增加 cards?: Array<{ type: "work" | "blog"; slug: string; title: string; description?: string; tags?: string[] }>
确定性派生(关键设计：不让 LLM 编造 slug):agent-factory 在工具循环结束后，把成功的 get_projects/get_works_detail/get_blog_posts/search_content 结果映射为卡片，slug 必须能在 content-index.ts 索引中验证，验证不过则丢弃；每条回答最多 3 张
前端:新组件 ContentCardRow(标题 + 标签 + 缩略，复用现有 works 封面)，点击经 wouter 跳转 /works/:slug、/blog/:slug;在 ChatModal 消息气泡下方渲染
Phase 5 — 访客探索进度个性化
新增 client/src/utils/visitor-progress.ts:localStorage 存储 { visitedRooms, discoveredCharacters, askedQuestions, lastSession },切房/提问时更新；纯客户端、无 PII、无服务端持久化
ChatModal.tsx:真正发送 visitedRooms/discoveredCharacters(接活 schema 死参数)；重开聊天窗时若有上次会话，展示"继续上次对话"入口
服务端：supervisor 注入一行进度上下文("访客已探索：builder、ai;尚未访问：visual…"),suggestedNextCharacterIds 优先推荐未访问房间 — 让"推荐下一个房间"从静态变成真的个性化