# Character Placeholder (Legacy)

The previous `server/agents/character` directory was an empty placeholder.
It has been moved to legacy to reduce structural noise.

If character-specific runtime modules are introduced in the future, create a new
active directory under `server/agents/character` with concrete implementation files.
