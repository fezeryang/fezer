# Orchestrator V1 (Legacy)

This folder contains the old orchestrator node/state implementation:

- `nodes.ts`
- `state.ts`

These files are retained for history and rollback investigation only.
They must not be imported by active runtime code.
