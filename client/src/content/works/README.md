# Works Content Directory

Place portfolio project markdown files here. Each file should have frontmatter with at minimum:

```yaml
---
title: "Project Name"
description: "One-paragraph summary for card display"
---
```

## Naming Convention

Use descriptive slugs without date prefix:
- `kinetic-typography.md`
- `spatial-audio-installation.md`

## Content Status

Currently **empty** — placeholder for static project descriptions that will replace tRPC `works.list` calls in Portfolio.tsx.

See `../README.md` for full schema reference.
